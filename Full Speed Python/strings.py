s = (input("Ingresa a string): ")) # se pide al usuario que ingrese una string

s = s[:1] # se toma el primer caracter de la string
s=  s[0] # se toma el primer caracter de la string
s = s[1:] # se toma el resto de la string
print(s)
